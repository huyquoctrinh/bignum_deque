#include<iostream>
#include<string.h>
#include<string>
#include<vector>
#include <algorithm>
#include <sstream>
#include<deque>
#include <iterator>
using namespace std;
bigint max(bigint* a, int n);
bigint min(bigint* a, int n);
bigint abs_big(bigint a);
#pragma once
